Parallel processing, modern and fast Task Runner, written in Rust

Usage: rusk [OPTIONS] [TASKNAME]...

Arguments:
  [TASKNAME]...
          Names of tasks to be executed

Options:
      --info <INFO>
          Print information

          Possible values:
          - tasks:   Print the task list
          - project: Print information of the current project in JSON

  -v, --verbose
          Log output in detail

      --completion <SHELL>
          Generate shell completion
          
          [possible values: bash, elvish, fish, powershell, zsh]

  -h, --help
          Print help (see a summary with '-h')

  -V, --version
          Print version
